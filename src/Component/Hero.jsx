import React from 'react';
import NotFound from './NotFound';

const Hero = () => {
    const clickbtn = ()=>{
        alert("clicked!")
    }
    return (
        <div>
            < NotFound/>
        </div>
    );
};

export default Hero;